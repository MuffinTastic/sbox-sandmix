using System.IO;

namespace SandMix;

public static partial class SoundLoader
{
	private static void LoadFromWav( Stream stream, out SoundData soundData, bool inVsnd = false )
	{
		if ( !inVsnd )
		{

		}
	}
}
